<%= simple_form_for(poem) do |form| %>
  <% if poem.errors.present? %>
    <%= render "errors", object: poem %>
  <% end %>
  <%= form.input :title %>
  <%= form.input :status, collection: Poem::POEM_STATUSES %>
  <%= form.button :submit %>
<% end %>
